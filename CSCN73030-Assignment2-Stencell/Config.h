#pragma once

// A header file seems like the easiest way to share preprocessor directives between .cpp files
// The other way is through the compiler configuration settings in VS and that doesn't seem as portable.

#define PRERELEASE

